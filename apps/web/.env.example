VITE_API_TARGET=http://localhost:3000

# Must match apps/api CLIENT_ID when client guard is enabled
WEB_API_CLIENT_ID=

# Must match apps/api CLIENT_SIGNING_SECRET when client guard is enabled
WEB_API_CLIENT_SECRET=

# Used by scripts/docker-push.js
DOCKER_IMAGE_NAME=
